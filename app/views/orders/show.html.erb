
<section class="orders-show">

<<<<<<< HEAD
<% @line_items.each do |item| %>
  <% p item %>
 <%= end %>
<%= p @line_items = @order.line_items %>
<% puts "ordeeerrrrrrrrrrrrr" %>
<% p @order %>


=======
>>>>>>> feature/order-details


  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>

  <div class="order-summary jumbotron">
    <h4>Thank you for your order - <%= @order.email %>!</h4> 
  </div>

  <div class="panel panel-default items">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th colspan="2">Items</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% @line_items.each do |item| %>
          <%= render 'orders/item', object: item %>
        <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th colspan="3">
            TOTAL:
          </th>
          <th>
            <%= @order.total_cents / 100.0 %>
          </th>
        </tr>
      </tfoot>
    </table>
  </div>

  


  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary' %>
  <h3> For more information please visit our about page. </h3>

</section>