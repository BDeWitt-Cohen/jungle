<section class="products-index">

  <header class="page-header">
    <h1>Products</h1>
  </header>

  <% flash.each do |type, msg| %>
    <div>
      <%= msg %>
    </div>
  <% end %>

  <div class="products">
    <%= render @products %>
  </div>

</section>
